spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/janberend/nexttowatch-config
          clone-on-start: true
          default-label: main
server:
  port: 8888
